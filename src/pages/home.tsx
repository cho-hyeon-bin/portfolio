import React, { useEffect, useRef } from 'react';
const HomeComponent: React.FC = () => {
	useEffect(() => {
		
	}, []);

	return (
		<div>
			<p>test</p>
		</div>
	);
};

export default HomeComponent;